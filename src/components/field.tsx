type FieldType = {
  id: string;
  label?: string;
  value: string;
  onChange: (value: string) => void;
};

const Field = ({ id, onChange, value, label }: FieldType) => {
  return <></>;
};

export default Field;
